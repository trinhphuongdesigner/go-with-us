# CareerMate v2 frontend

Next.js 16 + React 19 + Tailwind CSS 4 foundation for CareerMate v2.

```bash
cp .env.example .env.local
npm install
npm run dev
```

The demo account picker is deterministic and enabled only when `NEXT_PUBLIC_DEMO_MODE=true`.
No real employee data or credentials are included.
