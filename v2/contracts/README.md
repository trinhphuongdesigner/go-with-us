# CareerMate v2 contracts

`openapi.json` là snapshot chuẩn cho client frontend. Tạo hoặc kiểm tra snapshot bằng:

```bash
cd v2/backend
python -m scripts.export_openapi
python -m scripts.export_openapi --check
```
